# Basic example


# Outputs
```
>  terraform apply

Apply complete! Resources: 0 added, 0 changed, 0 destroyed.

Outputs:

module_tags = {
  "tags" = {
    "terraform_applied_at" = "2019-11-14T16:03:49Z"
    "terraform_managed" = true
    "terraform_workspace" = "default"
  }
}
```
